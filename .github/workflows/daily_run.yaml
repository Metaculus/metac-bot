name: run daily

on:
  schedule:
    - cron: "0 18 * * *" # every day, 10:40 PST
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v3
      - uses: actions/setup-python@v4
        with:
          python-version: "3.11"
      - name: Install poetry
        uses: snok/install-poetry@v1
        with:
          virtualenvs-create: true
          virtualenvs-in-project: true
          installer-parallel: true
      - name: Load cached venv
        id: cached-poetry-dependencies
        uses: actions/cache@v2
        with:
          path: .venv
          key: venv-${{ runner.os }}-${{ steps.setup-python.outputs.python-version }}-${{ hashFiles('**/poetry.lock') }}
      - name: Install dependencies
        if: steps.cached-poetry-dependencies.outputs.cache-hit != 'true'
        run: poetry install --no-interaction --no-root
      - name: Run all 3 bots
        if: always()
        run: |
          poetry run python forecast_bot.py --metac_token_env_name METACULUS_TOKEN_B1 --llm_model gpt-4o --use_perplexity --number_forecasts 5
          poetry run python forecast_bot.py --metac_token_env_name METACULUS_TOKEN_B2 --llm_model gpt-3.5-turbo --use_perplexity --number_forecasts 5
        env:
          METACULUS_TOKEN: ${{ secrets.METACULUS_TOKEN }}
          METACULUS_TOKEN_B1: ${{ secrets.METACULUS_TOKEN_B1 }}
          METACULUS_TOKEN_B2: ${{ secrets.METACULUS_TOKEN_B2 }}
          METACULUS_TOKEN_B3: ${{ secrets.METACULUS_TOKEN_B3 }}
          PERPLEXITY_API_KEY: ${{ secrets.PERPLEXITY_API_KEY }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
          TOURNAMENT_ID: ${{ vars.TOURNAMENT_ID }}
          API_BASE_URL: ${{ vars.API_BASE_URL }}
